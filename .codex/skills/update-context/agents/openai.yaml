interface:
  display_name: "Update Context"
  short_description: "Refresh docs/00-context from input"
  default_prompt: "Use $update-context to update docs/00-context files from a project description with gap-driven clarification questions."
dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Serena MCP server for symbol-aware navigation and edits"
policy:
  allow_implicit_invocation: false
