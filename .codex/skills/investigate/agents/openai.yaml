interface:
  display_name: "Investigate Failures"
  short_description: "Diagnose failures from output or issue description"
  default_prompt: "Use $investigate to analyze command output or a plain issue description, rank root-cause hypotheses, and propose durable fixes without making edits."
