interface:
  display_name: "Implement Plan"
  short_description: "Execute approved plan safely"
  default_prompt: "Use $implement-plan-safe to execute the approved plan with scoped edits, validations, and no-side-effect guardrails."
dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Serena MCP server for symbol-aware navigation and edits"
policy:
  allow_implicit_invocation: false
