# Global Security Blueprint

> Machine-managed by `tools/pc-prepare-features`. Edit PRD/context and rerun instead of hand-editing this file.

## Security scope boundaries

- Run `make prepare-features` to generate/update this project-scoped security baseline.
- Keep this document focused on repository-specific risks, controls, and evidence expectations.

## Threat model and attack surface

- Initial baseline placeholder. Run `make prepare-features` to populate project-specific details.

## Feature security focus map

- Initial baseline placeholder. Run `make prepare-features` to populate project-specific details.

## Security controls for this project

- Initial baseline placeholder. Run `make prepare-features` to populate project-specific details.

## Verification and evidence

- Initial baseline placeholder. Run `make prepare-features` to populate project-specific details.

## Alignment anchors

- Initial baseline placeholder. Run `make prepare-features` to populate project-specific details.
