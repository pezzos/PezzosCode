# CI/Test Autofix Prompt (pc-feature)

```
Follow docs/04-process/ticket-execution-protocol.md.
Fix ONLY the issues reported by the failing command. Keep scope tight.
Do not change unrelated behavior.
When used in precommit: run with vanilla Codex config (no profiles, no Serena MCP).
Do not update decision/implementation/validation logs for precommit-only fixes.
Step: {STEP}
Command: {COMMAND}
Command output:
{OUTPUT}
Ticket: {TICKET}
Worklog: {WORKLOG}
```
