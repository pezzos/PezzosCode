# Planner Allowed Tests Update Prompt

Allowed Tests must list a light, fast smoke command that exists.
Do not use `make ci`, `make feature`, or `pc-feature`.
Return ONLY the Allowed Tests section body (bulleted commands).

Work Item ID: {work_item_id}
Issues: {issues}
